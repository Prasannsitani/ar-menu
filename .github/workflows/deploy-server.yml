name: Build & Deploy Server
on: [workflow_dispatch]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          context: auth-validation
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: List All Droplets
        run: |
          doctl compute droplet list
          doctl compute ssh-key list
        # doctl compute ssh testing-server
        # ls

      # - name: Connect to Droplet
      #   run: ssh ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} "echo 'Connected to Droplet!'"
          


      # context: dop_v1_4202c82f6b337bbfb1070488e476e111380b65940466c319c065d8bdb294cf92

      # dop_v1_c9fe14ea5297d2d32c93ed930c0383cd00fb9d2d565e3e4c7a903993747907bf